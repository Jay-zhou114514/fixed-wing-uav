# 项目状态

更新时间：2026-09-18
当前阶段：**V2.1 冻结已批准；v1.3 已发布；EXP-27A 的 τ bug 已修复并验证**
下一步实验：**EXP-FW-V1-27A 正式运行**（脚本待按修正后的 τ 重写）
主实验：**EXP-FW-V1-26**（方向依赖延迟预算，闭环）

## 进度快照（2026-09-18 凌晨，明天从这里继续）

### 本轮完成

| # | 工作 | 产出 | 状态 |
| --- | --- | --- | --- |
| 1 | 整理 v1.3 计划 | 加第 0 节执行快照 + 第 13 节双轨申报 | ✅ 已推送 |
| 2 | Decision Log | `docs/DECISIONS.md` FW-D-006~013（8 条） | ✅ 已推送 |
| 3 | 标签 | `v2.1-freeze`、`v1.3-plan` | ✅ 已推送 |
| 4 | 申请书 V0.1 | `docs/application/DACHUANG_APPLICATION_V0.1.md` | ✅ 已推送 |
| 5 | paper-spine 技能 | 装入 `~/.zcode/skills/`（164 文件，frontmatter 校验通过） | ✅ |
| 6 | EXP-27A 诊断 | **τ 掠射 bug 已定位并修复** | 🔶 待提交 |
| 7 | τ 三件套验证 | 负/正对照 0.00e+00；vs 暴力参照 17.3 万组 0 超差；vs EXP-27 最大偏差 **1.4e-14 m** | 🔶 待提交 |
| 8 | 规范升级 | `FROZEN_PROTOCOL` 12.1（三件套）/12.2（良定义）/12.3（机动模型） | ✅ 已推送 |
| 9 | K4 界定语 | 加"首次外摆，非永久 contain" | ✅ 已推送 |

### 下一步（明天唯一动作）

**EXP-FW-V1-27A 正式运行。** 步骤：

1. **重写** `src/exp_fw_v1_27a_simultaneous.py`：
   - τ 用修正版（`notes/verify_tau_fix.py` 的模式 A）；
   - 逐边（EXP-27 口径，各边取最优 sign）vs 同时（单一 sign 满足所有边）；
   - `V(x0,ψ0) = min_sign max_e [pen_e − room_e]`；同时充分 ⟺ 全部 V ≤ 0；
   - 位置：缩放层顶点（主分析）+ 边中点（补充）；
   - **三件套必带**：负对照（直线边界应无违反）+ 正对照（判据须有检出能力）
     + 交叉验证（vs EXP-27 的 `required_at`）。
2. 运行：54 构型 × 4 风速比（w = 0, 0.1, 0.3, 0.5），含指向顶点的精确航向。
3. 写三层报告 `experiments/EXP-FW-V1-27A-simultaneous.md`。
4. 判定 **H-A**（逐边 ⇒ 同时成立）或 **H-B**（存在同时违反）。
5. 提交推送（分支 `audit/literature-decision-chains`，main 不动）。

**关键提醒**：EXP-27A 此前失败四次，**每次都靠自检/交叉验证拦下**。
本次重写必须先把三件套装上再跑主扫描——**不要先看主结论**。

### 已定的机动模型与视界（EXP-26 也复用）

> **视界** = **首次外摆**（`τ = 首个 v·n ≤ 0` 之前），与 EXP-21/27 同口径；
> **机动** = 单次最大速率转弯（接管后实施），之后由任务规划层接管。

理由：常值风 + 持续盘旋 ⇒ 顺风漂移必出界（穿透量随圈数**线性增长**：
w=0.3 时 1 圈 77.8 m → 10 圈 777.6 m），不构成有效任务假设。详见
`experiments/EXP-FW-V1-27A-failure-diagnosis.md` 第 3 节。

### 关键认知更正（本轮最重要的发现）

**我之前把 EXP-27A 判为"概念阻塞"是错的。** 真实根因是 τ 口径的**实现 bug**：
`v(0)·n = 0` 的**掠射**（相切）情形被误判为"已在后退"，导致穿透量错误（最大偏差 126 m）。
修正后与 EXP-27 逐点一致（1.4e-14 m）。

**EXP-27 的数值与结论不受影响**——它取 `max over ψ0`，最不利的 ψ0 都是"正在接近"的，
恰好绕开了该退化。**K1–K5 全部有效**，仅 K4 需带"首次外摆"界定语。

**元教训**：失败两次后就该停下来做**独立诊断**（暴力参照），
而不是连续改写判据、最终误判为"概念问题"。

## 负责人决定（2026-09-17，已生效）

| 项 | 决定 |
| --- | --- |
| V2.1 冻结（含删除 15 条 claim） | **批准** |
| EXP-26 为主实验 | **确认** |
| EXP-FW-V1-27A | **必须先做**（硬前置） |
| EXP-FW-V1-27B | **降级为后续**（不再阻塞） |
| Kim 2022 全文 | **继续获取，但不阻塞** |
| 研究计划 v1.3 | **批准发布** |

**随之生效**：定位正式表述为 safety **margin characterization**（非 buffer sizing）；
K6 措辞改为"解析式风感知围栏构造的安全裕度特性尚不清晰"，
并附 Kim 定位区分的固定表述（见冻结文件 2.4 节）。

## 一句话状态

研究对象、claim 边界与核心假设已全部冻结（V2.1），执行计划 v1.3 已发布。
**下一步唯一动作：EXP-FW-V1-27A**（纯几何、成本极低、检验 EXP-27 的逻辑缺口）。

## V2.1 冻结要点

| 项 | 内容 |
| --- | --- |
| **核心假设 H-core** | 该构造在"瞬时转弯"理想模型下恰好充分（垂直风向余量为 0），**因此在真实滚转动力学下必然不足**；不足量等价于一个滚转时间常数 |
| **H1** | `τ_crit(n) = margin(n)/(Va + W·n)`；垂直风向方向 = **0**（等级 C） |
| **H2** | 沿风向的 `τ_crit` 与空速无关（等级 C） |
| **H3** | 真实滚转下 `τ_crit = τL* − τ_roll`；垂直风向方向为负（**零延迟也不足**）（等级 C） |
| **删除的 claim** | 15 条（D1–D15），含"首次验证充分性""首次提出延迟预算""方法创新"等 |
| **保留的 claim** | 5 条等级 A（K1–K5）+ 2 条限定性（K6/K7） |
| **主实验** | EXP-FW-V1-26 |
| **前置检查** | EXP-FW-V1-27A（逻辑缺口）、EXP-FW-V1-27B（凹多边形） |

**冻结规则**：实验结果**可以**改变"下一步做什么"，**不得**改变研究问题与 claim 边界（见冻结文件第 8 节）。

## V2.1 新发现：EXP-27 的一处逻辑缺口（EXP-FW-V1-27A）

EXP-27 的 P2 检查的是**逐边独立**条件（每条边各自允许选最有利的转弯方向）。
但闭环安全要求**同时**满足所有边（单一转弯方向）。**若不同边的最优方向不同，
逐边检查不足以保证同时条件。** 该缺口与凹多边形无关，可能在凸多边形顶点附近出现。

**这不构成对 EXP-27 结论的反驳**——K2/K3/K4 是对"逐边安全裕度"的陈述，该陈述成立；
本条指出的是"逐边 ⇒ 同时"这一步未经检验。已列为 EXP-26 的硬前置。

## 当前主线的核心问题

> 固定翼无人机电子围栏缓冲距离在风扰、控制延迟和几何变化条件下，
> 其安全裕度如何变化？是否存在可解析的边界规律？

## V2.0 设计（已定稿）

见 `docs/plans/V2_DESIGN.md`，回答四个问题：
必须补哪些实验 / 申报书创新点如何形成 / 如何升级为研究项目 / 会议论文路线。

**优先级**（负责人原排序 + 本设计的建议）：

| 任务 | 负责人原排序 | 本设计建议 | 差异理由 |
| --- | --- | --- | --- |
| EXP-FW-V1-27B（凹多边形与展平） | — | **★★★★★ 建议最优先** | 唯一可能推翻"充分"结论之处；纯几何、成本低；影响后续设计 |
| EXP-FW-V1-26（越界闭环仿真） | ★★★★★ | ★★★★★ | 不变；把几何量接到任务层 |
| τL\* 理论扩展 | ★★★★★ | ★★★★ | 部分已由 EXP-27 回答（方向结构） |
| 风估计误差（EXP-28） | ★★★★ | ★★★★ | 不变 |
| 高保真仿真验证 | ★★★★ | ★★★ | 属期刊扩展，不阻塞会议线 |
| 转向 Future Feasibility | ★★★ | ★★★ | 保持不变（第三优先级） |

## 已完成

### 研究级资产（等级 A）

- [x] **EXP-FW-V1-27**（2026-09-17）：**安全裕度的方向结构**
  - `margin(沿风向) = δu = R`，**与风速无关**（与 EXP-22 互证）
  - `margin(垂直风向) = 0`，**精确紧**（解析核验 7.105e-15 m）
  - 零裕度方向由"**边法线 ⊥ 风向**"决定，**与多边形形状复杂度无关**
  - 三个自检全通过；125 构型 × 3 个风速比；P1–P6 全部通过
  - **仅限凸多边形**；凹多边形与展平登记为 EXP-FW-V1-27B
- [x] **EXP-FW-V1-23**（2026-09-17）：**延迟预算法则 `τL* = 1/(ω(1+w))`，与空速无关**；
  45 点网格偏差 0.000

### 验证级资产（等级 A/B）

- [x] EXP-FW-V1-21：缓冲构造在常值风下充分；δd 为精确最坏穿透量（H1/H2/H3 全通过）
- [x] EXP-FW-V1-22：沿风向余量**恒等于 δu**，与风速无关；静风余量为零
- [x] EXP-FW-V1-20：曾报告"w\* ≈ 0.32" → **已撤回**（判据 artifact），记录保留

### 文献工作

- [x] **`docs/LITERATURE_AUDIT_V2.md`**：立项理由（"充分性未被验证"）的判定
  - **已读 JAIS 2020 全文含第 III 节**（此前未读），关键词计数：
    `guarantee` 0、`delay` 0、`uncertainty` 0、`wind estimat` 0
  - **结论：立项理由成立**
  - **但发现 Thomas 2024 已发表"延迟预算"概念**（`s_t = V·t_c`），
    故"首次提出"不可主张；差异为"精确/含风/与空速无关"
  - **Kim et al. 2022 全文未获取**（付费）→ 表述须限定为"该构造"
  - 按 `research-stack-router` 的 **Phase B 门禁：未通过**（阻塞项 = Kim 2022）
- [x] `docs/LITERATURE_MAP.md` 第 11 节已按 EXP-27 结果修正
  （原"矩形是最坏情形"改为"零裕度方向由边法线与风向的关系决定"）

### 规范与平台

- [x] `docs/FROZEN_PROTOCOL.md`（G2）、`docs/AI_USE_DISCLOSURE.md`
- [x] `src/exp_fw_v1_27_general_polygon.py`（含解析 τ 实现与三个自检）

## 未完成（当前阻塞项）

- [ ] **EXP-FW-V1-27B**：凹多边形与顶点展平（**唯一可能推翻"充分"之处**）
- [ ] **EXP-FW-V1-26**：越界事件的闭环仿真（把几何量接到任务层）
- [ ] **Kim et al. 2022 全文**（支付壁垒）：申报书能否写"首次检验"的前提
- [ ] EXP-FW-V1-28：风估计误差进入延迟预算
- [ ] `FROZEN_PROTOCOL.md` 中 `[待实测冻结]` 的字段补全
- [ ] 研究计划 v1.3 发布（触发条件：EXP-27B 或 EXP-26 完成 + 主线确认）

## 下一步（建议顺序，需负责人确认）

1. **EXP-FW-V1-27B**：凹多边形与展平。理由：纯几何、成本低，
   且是唯一可能推翻"构造充分"的地方；若发现缺口，整个故事形态改变，
   后续实验设计需相应调整（越早知道越好）。
2. **EXP-FW-V1-26**：闭环仿真。理由：把 `τL*` 与方向结构接到任务层后果；
   没有它，会议论文只有 C1+C2（几何层）。
3. **Kim 2022 全文获取**：与上述并行（不需要仿真环境）。

## 已知局限

- **仅凸多边形**：EXP-27 未覆盖凹多边形与顶点展平（JAIS 第 III.B 节为回收面积
  而做展平，会**减少**缓冲，故凹多边形可能有真实缺口）。
- **Kim 2022 全文未读**（付费）：**不阻塞**任何实验与 v1.3；
  措辞改用固定表述"解析式风感知围栏构造的安全裕度特性尚不清晰"
  （"Kim studies uncertainty-aware buffer sizing, while ..."），
  **不得**写成"Kim 未验证充分性"或"无人验证过"。
- **EXP-27 的"逐边 ⇒ 同时"这一步未经检验**（EXP-FW-V1-27A）：
  在 27A 完成前，K2/K3/K4 不得升级为"该构造在所有情形下充分"的表述。
- 3 自由度水平面点质量模型；不含高度、气动、失速、滚转瞬态。
- 常值风；阵风、风切变、风估计误差、跟踪误差均未处理。
- **"零冗余导致任何未建模效应直接产生缺口"是推论，等级 C**，未检验（EXP-26 的任务）。
- **`τL*` 与零冗余边界的关联（"剩余时间"）是解释性主张，等级 C**。
- 失效侧候选形式（`notes/LATENCY_FAILURE_SIDE_NOTE.md`）等级 **C**，未独立验证。
- 纯仿真；真机不作为结题前提（FW-D-002）。

## 学生目标与背景

- 自动化专业，本科大三，预计 2028 年毕业。GPA 3.1/5.0；英语六级 529。
- 目标：通过持续、可验证的项目成果提升竞争力，准备申请港科大（广州）红鸟挑战营及红鸟硕士项目。
- 当前资源：电脑、每天至少 2 小时，周末更多；暂无实验室、硬件、竞赛或科研基础。
- 载体路径：广工校级大创 → 省级/国家级 → 会议论文 → 期刊扩展。

## 两个项目的分工

| 项目 | 研究对象 | 规范 |
| --- | --- | --- |
| `motor-health-monitor`（项目一） | 轴承振动异常检测的评价可靠性 | 本项目的规范来源 |
| `fixed-wing-uav`（项目二，本仓库） | 固定翼未来可行性驱动决策 | 沿用项目一全套规范 |

两个项目共用同一套方法论：预注册、证据等级 A–D、三层次报告、
失败保留、版本化计划、数字可追溯、AI 使用披露。
